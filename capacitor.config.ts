import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zeus.miinventarioapp',
  appName: 'Mi Inventario App',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
